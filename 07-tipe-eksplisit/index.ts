// tipe deklarasi tipe data string secara tidak langsung (implisit)
// let nama = "Feliks" 

// tipe deklarasi tipe data string secara langsung (eksplisit)
let nama: string = "Ujang" 
let umur: number = 21;
let isAktif: boolean;
let kegiatan: string[] = []; // tipe data string of array